pub mod hosting; // Cada módulo dentro de otros módulos pueden tener su propio archivo dentro de una carpeta con el nombre del módulo padre.

mod serving {
    fn take_order() {}

    fn serve_order() {}

    fn take_payment() {}
}

